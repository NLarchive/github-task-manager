<svg width="100" height="100" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-width="5" stroke-linecap="round" stroke-linejoin="round">
    <title>Woman Chief of People Analytics Icon</title>
    <style>
        /* Optional: Define colors for dark/light mode */
        @media (prefers-color-scheme: dark) {
            :root { color: #e0e0e0; } /* Light grey for dark mode */
        }
        @media (prefers-color-scheme: light) {
            :root { color: #333; } /* Dark grey for light mode */
        }
    </style>
    <!-- Woman Figure (Head and Shoulders) -->
    <circle cx="50" cy="35" r="12" />
    <path d="M30 70 Q50 55 70 70 Z" />

    <!-- Globe Element (Simplified) -->
    <circle cx="50" cy="70" r="20" />
    <path d="M50 50 V 90 M30 70 H 70" />
    <path d="M38 60 Q50 65 62 60" />
    <path d="M38 80 Q50 75 62 80" />
</svg>