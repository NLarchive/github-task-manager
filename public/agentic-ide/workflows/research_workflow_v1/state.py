"""Python placeholder for workflow state parity.

The JS runtime uses state.js. This file exists to keep the workflow's declared
file list consistent and support optional Python-side tooling.
"""

STATE_SCHEMA = {
    "topic": "string",
    "report": "string",
}
