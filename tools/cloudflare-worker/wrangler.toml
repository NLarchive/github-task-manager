name = "github-task-manager"
main = "worker.js"
compatibility_date = "2024-12-12"

# Environment variables (public, safe to expose)
vars = { ENVIRONMENT = "production" }

# Secrets (must be set with `wrangler secret put`)
# - GITHUB_TOKEN
# - ACCESS_PASSWORD_MASTER
# - ACCESS_PASSWORD_GITHUB_TASK_MANAGER
# - ACCESS_PASSWORD_AI_CAREER_ROADMAP
